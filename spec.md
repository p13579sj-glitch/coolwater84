# 스펙 - AI 코드 생성 리뷰 자동화

> 본 문서는 `BRIEF.md`의 기획 요청 메모를 실행 가능한 스펙으로 구체화한 것이다.

## 1. 목적

엔지니어가 AI를 통해 코드를 생성/수정할 때, 요구사항이 정확히 전달·반영되었는지 단계별로 검증하고, 엔지니어가 전체 변경 내용을 빠르게 파악할 수 있도록 리뷰·기록 프로세스를 표준화한다.

해결 대상 문제 (BRIEF.md 참조):
1. 참조 클래스/프로젝트 동시 수정으로 인한 문제 파악 지연
2. 부서 코드 룰을 벗어난 코드 도입으로 인한 분석 시간 증가
3. 임의 패키지 설치/업데이트로 인한 호환성 문제
4. 변경량 과다로 인한 요구사항 반영 여부 확인 지연

근본 원인: 불명확한 요구사항, 요구사항 변질, 비합의 개발, 리뷰 불가 구조.

## 2. 용어 정의

| 용어 | 정의 |
|---|---|
| 작성 PC | 이 작업 환경. .NET SDK 없음. 코드 작성/수정/리뷰만 수행하며 `dotnet` 명령을 실행하지 않는다. |
| 검증 PC | 엔지니어가 별도로 운영하는 환경. 빌드/테스트/실행을 담당한다. |
| main agent | 개발·룰 준수·사전검토·리뷰 진행·기록을 총괄하는 에이전트. |
| sub agent / skill | 프로젝트 룰 분석, 개발 이력 기록 등 위임 가능한 단위 작업을 수행하는 에이전트. |
| Task | 세분화된 요구사항 단위. 1개 루프(사전검토 → 개발 → 리뷰 → 기록)로 처리된다. |
| 작성 완료 | 리뷰 Critical 0건 + 엔지니어 승인을 만족한 상태. |
| 검증 완료 | 검증 PC 빌드 성공 + 테스트 통과를 만족한 상태. |

## 3. 역할 분담

### 3.1 main agent
- 내부 룰 파일을 벗어나지 않는 범위에서 개발 진행
- Task별 수정 필요 영역과 사유를 사전 검토 후 엔지니어에게 공유
- 핵심 클래스 외 영역 수정 시 엔지니어 확인 절차 선행
- 패키지 업데이트/다운로드 절대 금지 (필요 시 엔지니어 수동 작업으로 전환 요청)
- Task 완료 판정 (작성 완료 / 검증 완료) 및 기록 파일 갱신
- 리뷰에서 3회 이상 반복 발생한 항목을 `CLAUDE.md`에 반영
- 검증 불가 환경 제약사항 준수 (§7)

### 3.2 sub agent / skill
- 프로젝트 전체 코드 분석 → 내부 룰 파일 생성/관리 (프로젝트당 1회성 생성, 이후 호출 방식으로 재사용)
- Task 단계 완료 시점마다 진행 결과/리뷰 내용을 기록 파일에 기록
- 리뷰 보고서 말미에 [검증 PC 확인 필요] 목록 작성

## 4. Task 루프 워크플로우

```
Task N: 사전검토 → 개발 → 리뷰 → 기록 → Task N+1: 사전검토 → ...
```

각 Task는 아래 4단계로 구성되며, 토큰 제약상 Task 단위로 루프를 완결한다(다음 Task로 넘어가기 전 컨텍스트를 정리).

1. **사전검토**
   - 대상 프로젝트에 요구사항 문서(§5)가 없으면 먼저 작성하고, 있으면 최신 상태인지 확인한다.
   - 수정/개발이 필요한 영역과 사유를 정리해 엔지니어에게 공유하고 합의한다.
   - Task당 변경 파일 수 상한을 이 단계에서 합의한다. **전역 기본값은 두지 않으며, 매 Task마다 새로 확인한다**(§12).
2. **개발**
   - 요구사항 문서(§5)와 내부 룰 파일(§6)에 정의된 범위·컨벤션을 벗어나지 않고 구현한다.
   - 핵심 클래스 외 수정이 필요해지면 즉시 중단하고 엔지니어 확인 요청
   - 패키지 설치/업데이트 불가, `dotnet` 명령 실행 불가
3. **리뷰**
   - 요구사항 문서(§5) 대비 구현 내용 매핑 확인 (요구사항 → 코드 반영 여부)
   - Critical/Major/Minor 등급으로 이슈 분류
   - [검증 PC 확인 필요] 목록 작성 (§9)
4. **기록**
   - 진행 결과와 리뷰 내용을 기록 파일에 추가 (§10)
   - 작성 완료/검증 완료 상태를 분리 기록
   - 리뷰 이슈 중 3회 이상 반복된 패턴은 `CLAUDE.md`에 룰로 추가

## 5. 요구사항 문서

- **목적**: Task별 실제 기능 요구사항(무엇을 왜 바꾸는지, 완료 기준)을 명문화해 "요구사항이 변질되지 않고 정확히 코드화되었는지" 리뷰 단계에서 대조할 수 있는 근거로 삼는다. BRIEF.md의 근본 원인 1번("명확하지 않은 요구사항")에 대한 직접적인 대응책이다.
- **범위**: 이 문서는 자동화 시스템 자체(`spec.md`, `backlog.json` 등)가 아니라, main agent가 실제로 개발을 진행하는 **대상 프로젝트**에 귀속된다.
- **저장 위치**: 대상 프로젝트 루트 기준 `documents/requirements/<Task ID>.md`.
- **생성 주체 및 시점**: main agent가 각 Task의 **사전검토** 단계에서 작성하거나 갱신하고, 엔지니어 승인을 받은 뒤 개발에 들어간다.
- **필수 항목**:
  - Task ID, 한 줄 요약
  - 요구사항 상세 (배경/무엇을/왜)
  - 완료 기준 (Acceptance Criteria)
  - 영향 범위(핵심 클래스 외 수정이 예상되면 명시)
  - 참조(있다면 외부 이슈 트래커 티켓 ID/링크 등)
- **활용**: 리뷰 단계(`review-checker`)는 이 문서를 기준으로 "요구사항 반영 매핑"을 수행하고, 기록 단계(`task-recorder`)는 이 문서의 한 줄 요약을 기록 파일에 인용한다.

## 6. 내부 룰 파일

- **목적**: 부서 코드 작성 컨벤션(예: 다중 리턴이 필요한 함수는 tuple 대신 전용 클래스를 생성하여 리턴)을 명문화하여 개발 시 참조.
- **생성 주체**: sub agent 또는 skill
- **생성 시점**: 프로젝트당 1회성. 이후 코드 구조 변경 등으로 재분석이 필요할 때 수동 호출.
- **적용 주체**: main agent가 개발 시 이 파일을 벗어나지 않는 범위에서 구현
- **형식(확정)**: 프로젝트 루트에 `RULES.md`로 저장. 규칙마다 근거 코드 위치(예시 파일:라인)를 함께 명시하여 추적 가능하게 한다.

## 7. 환경 제약

- 이 환경(작성 PC)에는 .NET SDK가 없으며, `dotnet` 명령을 실행하지 않는다.
- 컴파일러 검증이 필요한 판단은 추측하지 않고 **"미검증"**으로 명시하여 검증 PC로 이관한다.
- 회피 대상:
  - 확인되지 않은 API·오버로드 사용
  - 대상 프레임워크가 미지원하는 언어 기능
- 실제 빌드·테스트(검증)는 검증 PC에서 진행한다. 여기(작성 PC)에서 리뷰까지 문제 없이 통과하면, 이후 미진 사항 보완·재설계·개발은 검증 PC에서 자체적으로 이어간다. 오류를 이 환경으로 다시 가져와 리뷰를 반복하는 절차는 두지 않는다.

## 8. 리뷰 보고서 형식

- **이슈 등급(Critical/Major/Minor) 기준**: 전역 기본값을 두지 않는다. **매 Task 리뷰 단계에서 main agent가 엔지니어에게 개별적으로 확인**하고, 그 기준을 `review-checker`에 전달해 사용한다.

리뷰 보고서 말미에 아래 항목을 [검증 PC 확인 필요] 목록으로 첨부한다:

- 타입 추론이 불확실한 지점
- 시그니처 미확인 호출
- nullable 경고 예상 위치
- 신규·변경 파일 목록

## 9. Task 완료 판정 및 기록

Task 완료는 2단계로 분리 판정하며, 기록 파일에 두 상태를 각각 기록한다.

| 상태 | 판정 기준 |
|---|---|
| [작성 완료] | 리뷰 Critical 0건 + 엔지니어 승인 |
| [검증 완료] | 검증 PC 빌드 성공 + 테스트 통과 |

**저장 위치(확정)**: `documents/execute_report/<Task ID>.md`.

기록 파일에는 최소한 다음을 포함한다:
- Task ID / 요구사항 요약 (§5 요구사항 문서 인용)
- 사전검토 요약 (영향 범위, 변경 파일 수 상한 합의 내용)
- 리뷰 결과 요약 (등급별 이슈 수, [검증 PC 확인 필요] 목록)
- 작성 완료 / 검증 완료 각각의 날짜·판정자

## 10. CLAUDE.md 반영 규칙

- 리뷰에서 동일/유사 패턴의 이슈가 **3회 이상** 반복 발생하면 main agent가 해당 항목을 `CLAUDE.md`에 규칙으로 기록한다.
- 목적: 동일 지적을 반복하지 않도록 팀 컨벤션으로 승격.

## 11. 선택 기능 (있으면 좋은 것)

- **데일리 개발 진행 이력 관리 시트** (프로젝트 관리용)
  - 포함: 개발 내용, 코드 수정량, 개발 제안 및 수락 현황
  - 제외: 코드 내용 자체는 기재하지 않음
  - **형식(확정)**: 로컬 마크다운 파일. `documents/daily-task_report/<YYYY-MM-DD>.md`. 외부 시스템 연동 없음.

## 12. 결정 사항 (엔지니어 확정, 2026-08-13)

- 룰 파일·요구사항 문서·기록 파일 저장 위치/포맷: `RULES.md`(§6), `documents/requirements/<Task ID>.md`(§5), `documents/execute_report/<Task ID>.md`(§9) — 전부 Markdown.
- 데일리 이력 시트 형식: 로컬 마크다운, `documents/daily-task_report/<YYYY-MM-DD>.md`, 외부 연동 없음(§11).
- **Task당 변경 파일 수 상한**: 전역 기본값 없음. **매 Task 사전검토 단계에서 엔지니어에게 개별 확인**한다(§4).
- **리뷰 이슈 등급(Critical/Major/Minor) 기준**: 전역 기준 없음. **매 Task 리뷰 단계에서 엔지니어에게 개별 확인**한다(§8).
- **sub agent(룰 분석)와 main agent(개발) 간 호출 인터페이스**: 룰 분석은 Claude Code **skill**(`analyze-project-rules`, 사람이 `/analyze-project-rules`로 명시 호출), 기록·리뷰는 **subagent**(`task-recorder`, `review-checker`, main agent가 Agent 도구로 호출)로 확정.

미결정 사항 없음.
